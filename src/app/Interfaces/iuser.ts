export interface Iuser {
    // we must create our interface to be the one for one with our backend model.
    id:Number,
    firstName:String,
    lastName:String,
    userName:String,
    password:String,
}